module.exports = {
    name: "nerooc",
    aliases: [""],
    category: "fun",
    description: "-",
    usage: "<input>",
    run: async (client, message, args) => {
        const msg = await message.channel.send("nerooc ty głupi chuju");
    }
}